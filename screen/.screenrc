startup_message off
termcapinfo xterm* 'hs:ts=\E]0;:fs=\007:ds=\E]0;screen\007:ti@:te@'
shell -bash
layout autosave on
defscrollback 100000

screen -t server
select server
wrap off
source $HOME/.screen/$SPEED

screen -t console
select console
wrap off
stuff "while [[ -e /tmp/screen.lock ]]; do echo sleeping; sleep 1; done; ^M"
stuff "cd ~/Handshake/handshake; ^M"
stuff "set -a; . .env; set +a ^M"
stuff "rails c ^M"

screen -t foreman
select foreman
stuff "cd ~/Handshake/handshake; ^M"
stuff "while [[ -e /tmp/screen.lock ]]; do echo sleeping; sleep 1; done; ^M"
stuff "foreman start; ^M" # use .foreman to specify procfile and exclude web

screen -t guard
select guard
stuff "cd ~/Handshake/handshake; set -a; . .env; set +a; ^M"
stuff "while [[ -e /tmp/screen.lock ]]; do echo sleeping; sleep 1; done; ^M"
stuff "bundle exec guard ^M"

screen -t test
select test
stuff "cd ~/Handshake/handshake; nvm use; yarn run local-test; ^M"

screen -t office
select office
stuff "lsof -i tcp:3002 | awk 'NR!=1 {print \$2}' | xargs kill ^M"
stuff "cd ~/Handshake/handshake-office ^M"
stuff "set -a; . .env; set +a; ^M"
stuff "foreman start ^M"

screen -t storybook
select storybook
stuff "cd ~/Handshake/handshake; set -; . .env; set +a; ^M"
stuff "yarn run storybook ^M"

#screen -t wat
#select wat
#stuff "cd ~/Handshake/handshake ^M"
#stuff "bundle install ^M"
#stuff "bundle exec rake db:migrate ^M"
#split -v
#focus right
#screen -t huh
#select huh
#focus left



# start on the rails server
select 0

altscreen on
term screen-256color
bind ',' prev
bind '.' next
bind 'K' quit

# change the hardstatus settings to give an window list at the bottom of the
# screen, with the time and date and with the current window highlighted
hardstatus alwayslastline
hardstatus string '%{= kG}[%= %{= kw}%?%-Lw%?%{r}(%{W}%n*%f%t%?(%u)%?%{r})%{w}%?%+Lw%?%?%= %{g}][%{B} %m-%d %{W}%c %{g}]'
